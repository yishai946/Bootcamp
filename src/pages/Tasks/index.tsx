import { Box, Typography } from '@mui/material';

const Tasks = () => {
  return (
    <Box width="100%">
      <Typography variant="h2" fontWeight={700}>
        משימות
      </Typography>
    </Box>
  );
};

export default Tasks;
