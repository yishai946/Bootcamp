import { Typography } from '@mui/material';

const Home = () => {
  return (
    <div>
      <Typography variant="h2" fontWeight={700}>
        Home
      </Typography>
    </div>
  );
};

export default Home;
